function Footer() {
  return (
    <footer className="site-footer">
      <div>
        <strong>DNTU Forum</strong>
        <p>Không gian trao đổi học tập, hỏi đáp kỹ thuật và xử lý nội dung có kiểm duyệt.</p>
      </div>
      <div className="site-footer__meta">
        <span>Phạm vi hiện tại: User + Admin</span>
        <span>Workflow: Đăng bài → Chờ duyệt → Hiển thị</span>
      </div>
    </footer>
  );
}

export default Footer;
